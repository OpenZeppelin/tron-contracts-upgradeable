---
'openzeppelin-tron-solidity': patch
---

Run the test suite against `tronbox/tre` 2.0 (java-tron 4.8.2): pin the 2.0 image digest, refresh the `@openzeppelin/hardhat-tron` FullNode.jar patch that targets it, and pre-approve the `allowTvmPrague` / `allowTvmOsaka` proposals in the test containers.
