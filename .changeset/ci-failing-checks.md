---
---

ci: get the CI pipeline green — provision the patched TRE FullNode.jar as a shared artifact, compile the corpus once before the parallel tests, fix the upgradeable peer-dependency directory (`lib/tron-contracts`), enable via-ir for forge so halmos builds, and migrate the Certora formal-verification suite (patches, harnesses, specs) to the fork's TRC naming.
